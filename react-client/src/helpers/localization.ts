import { REGIONS, LOCAL_STORAGE } from '../constants';

// export const getCurrentLanguage = () => {
//   const language =
//     localStorage.getItem(LOCAL_STORAGE.LOCALIZATION) || REGIONS.en.key;
//   return language;
// };

// export const changeLanguage = (language: string) => {
//   if (language === getCurrentLanguage()) return;
//   localStorage.setItem(LOCAL_STORAGE.LOCALIZATION, language);
//   window.location.reload();
// };
